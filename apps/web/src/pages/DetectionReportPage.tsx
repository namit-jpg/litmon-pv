// The existing review surface already contains the audit-safe ICSR checklist
// and decision history.  Phase 3 deliberately extends it rather than creating
// a second, divergent article implementation.
export { default } from "./ArticlePage";
